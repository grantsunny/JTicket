apiVersion: v1
kind: Service
metadata:
  name: jticket
  namespace: jticket
spec:
  selector:
    app: jticket
    component: app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
