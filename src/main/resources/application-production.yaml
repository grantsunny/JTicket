spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://grantsunny.auth0.com/
          audiences: jticket-auth0-demo
      client:
        registration:
          jticket:
            provider: auth0
            client-id: sGIuOL9U1DtpD39kofVqAEdSW7BaLdRb
            client-secret: pJeoD-UJQ559icUZHA15J96-rsza8Xao4i8HU6oKzwrdhCEtfTXZXxRUkfHIK3xa
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            scope: openid,profile,email
        provider:
          auth0:
            issuer-uri: https://grantsunny.auth0.com/
  h2:
    console:
      enabled: false
  datasource:
    url: jdbc:postgresql://localhost:26257/TKT
    driver-class-name: new org.postgresql.Driver
    username: root
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      idle-timeout: 3000
      max-lifetime: 180000
      connection-timeout: 3000
  sql:
    init:
      mode: always
      schema-locations: classpath:postgresql.sql
      continue-on-error: true
ticket:
  oauth2:
    audience: jticket-auth0-demo
  user-token:
    required: true
    path: /api/orders
    name: x-ticket-userId
  sqlParams:
    SEATDETAILS: SEATDETAILS
    SEATSINEVENT: SEATSINEVENT
    SKU: SKU
